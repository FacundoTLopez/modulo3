import { createGlobalStyle } from "styled-components";

export const GlobalStyles = createGlobalStyle`
    *{
        margin: 0;
        padding: 0;
    }
`;

/**
 * GlobalStyles es una funcion, no es con parentesis pero con comillas invertidas ``, esto es un componente, asi que para usarlo lo importo al final de la app
 */
